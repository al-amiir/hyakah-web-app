export interface SideBArLinksInterface {
  icon: string;
  name: string;
  link: string;
}

export interface ProductListInterface {
  name: string;
  price: number;
  cost: number;
  quantity: number;
  images: string;
}
